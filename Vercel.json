{
    "version": 2,
    "builds": [
        {
            "src": "/flask_app/app.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/flask_app/app.py"
        }
    ]
}
